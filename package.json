{
  "name": "openwa-plugins",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Official & community plugins for OpenWA.",
  "license": "MIT",
  "scripts": {
    "build": "node package.mjs gsheets-logger",
    "catalog": "node scripts/catalog.mjs",
    "catalog:check": "node scripts/catalog.mjs --check",
    "test": "node --import tsx --test \"gsheets-logger/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
